import fs from 'node:fs';import path from 'node:path';const root=process.cwd(),bad=[],skip=new Set(['.git','node_modules','.tmp-test','dist','target','runtime'])
function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(skip.has(e.name))continue;const p=path.join(d,e.name),r=path.relative(root,p).replaceAll('\\','/');if(e.name.length>64)bad.push(`name >64: ${r}`);if(r.length>220)bad.push(`path >220: ${r}`);if(e.isDirectory())walk(p)}}walk(root);if(bad.length){console.error('AGMP Naming Gate FAIL');bad.forEach(x=>console.error(' - '+x));process.exit(1)}console.log('AGMP Naming Gate PASS')
